package model;

/**
 * The Direction enum class represents all possible directions in which the player can
 * move through the dungeon.
 */
public enum Direction {
  NORTH,
  SOUTH,
  EAST,
  WEST;
}
